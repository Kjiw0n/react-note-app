const colors = {
  transparentBackground: "rgba(255, 255, 255, 0.7)",
  white: "#FFFFFF",
  black: "#000000",
  red: "#FF0000",
  gray: "#808080",
};

const theme = { colors };

export default theme;
